// In production, texel will strip the 'texel/util/exporter'
// module to keep your bundle size smaller.
module.exports = {
  saveFile: optimizedAwayFunction,
  saveCanvas: optimizedAwayFunction
};

function optimizedAwayFunction () {
  /* This function has been optimized into a no-op by texel. */
  return Promise.resolve({});
}
